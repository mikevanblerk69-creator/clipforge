/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'your-project.supabase.co',
      'replicate.delivery',
      'pbxt.replicate.delivery',
    ],
  },
}

module.exports = nextConfig
